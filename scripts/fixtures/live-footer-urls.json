{
  "_comment": [
    "Every internal URL the LIVE Webflow site links from its own footer.",
    "This is the set tncld#44's DNS cutover makes the rebuild responsible for:",
    "the moment tncld.com points at Next.js, any URL here without a route or a",
    "redirect is a hard 404 for bookmarks, emails, directory listings and search",
    "results. scripts/test-live-urls.mjs asserts all of them still resolve.",
    "",
    "Captured 2026-09-02 from https://tncld.webflow.io/about (the Webflow-hosted",
    "original; tncld.com is deliberately NOT allow-listed because #44 turns it",
    "into the rebuild). The footer is one shared Webflow symbol, so any page",
    "yields the same list. Regenerate with:",
    "",
    "  curl -sL https://tncld.webflow.io/about -o /tmp/live-about.html",
    "  node -e \"const h=require('fs').readFileSync('/tmp/live-about.html','utf8');\\",
    "    const i=h.search(/class=\\\"[^\\\"]*footer[^\\\"]*\\\"/);\\",
    "    console.log([...new Set([...h.slice(i).matchAll(/<a[^>]+href=\\\"(\\\\/[^\\\"]*)\\\"/g)]\\",
    "      .map(m=>m[1]))].join('\\\\n'))\"",
    "",
    "Match <a> only: the Hours block renders href on <div> elements carrying",
    "Webflow's own template URLs (/features, /premium). Those are not links and",
    "must not enter this list."
  ],
  "source": "https://tncld.webflow.io/about",
  "capturedAt": "2026-09-02",
  "urls": [
    "/",
    "/about",
    "/about/meet-the-doctors",
    "/about/technology",
    "/about/tour-our-office",
    "/about/training-center",
    "/contact",
    "/legal/accessibility-statement",
    "/legal/disclaimer",
    "/legal/hipaa-notice-of-privacy-practices",
    "/legal/notice-of-nondiscrimination",
    "/legal/privacy",
    "/legal/terms",
    "/patient-resources",
    "/patient-resources/membership-plan",
    "/patient-resources/new-patients",
    "/patient-resources/payments-and-insurance",
    "/request-appointment",
    "/services",
    "/services/cosmetic-dentistry",
    "/services/dental-implants",
    "/services/hybrid-dentures",
    "/services/invisalign",
    "/services/preventative-dentistry",
    "/services/restorative-dentistry",
    "/services/sedation-dentistry"
  ]
}
