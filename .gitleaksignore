# .gitleaksignore — acknowledged gitleaks findings.
# Format: <commit>:<file>:<rule>:<startline>  (see secret-scan-triage.sh).
#
# EMPTY on purpose since 2026-09-25 (brik-llm#3789). The six fingerprints that
# lived here pointed at commit 0c99e520 (dead Notion + Webflow tokens, and a
# dead Figma PAT). This repo's history was rewritten with
# `git filter-repo --sensitive-data-removal`, so that commit no longer exists
# on any branch and the fingerprints matched nothing.
#
# Before adding an entry, read brik-llm operations/security/when-to-rotate.md:
# this repo is PUBLIC, so a real value in history is a rotate-and-purge case,
# never an ignore line.
